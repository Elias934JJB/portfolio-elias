const pathname = new URL(Astro.request.url).pathname;

<nav class="bg-skin-fill border-skin-base sticky top-0 z-50 border-b">
  <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
    <div class="flex h-16 justify-between">
      <div class="flex">
        <div class="flex flex-shrink-0 items-center">
          <a href="/portfolio-elias/" class="text-xl font-bold text-skin-base">Portfolio</a>
        </div>
        <div class="hidden sm:ml-6 sm:flex sm:space-x-8">
          <a
            href="/portfolio-elias/mon-travail"
            class="inline-flex items-center border-b-2 px-1 pt-1 text-sm font-medium border-transparent text-skin-base hover:border-skin-accent/50 hover:text-skin-accent/90"
          >
            Mon Travail
          </a>
          <a
            href="/portfolio-elias/stage"
            class="inline-flex items-center border-b-2 px-1 pt-1 text-sm font-medium border-transparent text-skin-base hover:border-skin-accent/50 hover:text-skin-accent/90"
          >
            Stage
          </a>
          <a
            href="/portfolio-elias/veille"
            class="inline-flex items-center border-b-2 px-1 pt-1 text-sm font-medium border-transparent text-skin-base hover:border-skin-accent/50 hover:text-skin-accent/90"
          >
            Veille
          </a>
        </div>
      </div>
    </div>
  </div>
</nav>

<script>
  // Add active state to navigation links
  const links = document.querySelectorAll('nav a');
  const currentPath = window.location.pathname;

  links.forEach(link => {
    if (currentPath.includes(link.getAttribute('href'))) {
      link.classList.remove('border-transparent', 'text-skin-base', 'hover:border-skin-accent/50', 'hover:text-skin-accent/90');
      link.classList.add('border-skin-accent', 'text-skin-accent');
    }
  });
</script> 